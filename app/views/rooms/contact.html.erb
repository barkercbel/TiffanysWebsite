

<div class="room-container" >
	<div class="statement-container" >
		<p><%= raw @info.value %></p>
	</div>
</div>

<div id='admin-links'>
	<% if !session[:user_id].nil? && @current_user.admin %>
		<%= link_to 'Edit', edit_room_info_path(@info) %>
	<% end %>
</div>
